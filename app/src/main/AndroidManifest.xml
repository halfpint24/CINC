<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.cinc">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true">
        <activity android:name=".VideoPage1Activity"></activity>
        <activity android:name=".ThirtySecondsActivity" />
        <activity android:name=".VSActivity" />
        <activity android:name=".TutorialPage2Activity" />
        <activity android:name=".TutorialPage1Activity" />
        <activity android:name=".TutorialActivity$TutorialPage1Activity" />
        <activity android:name=".AboutActivity" />
        <activity android:name=".ForumActivity" />
        <activity android:name=".GameActivity" />
        <activity android:name=".VideoActivity" />
        <activity android:name=".TutorialActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>